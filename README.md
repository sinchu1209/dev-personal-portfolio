Personal Portfolio
Personal portfolio project built using Vite + React + Tailwind CSS.

## Project Setup
# Create Vite project
npx create-vite@latest

# Move into project folder
cd dev-personal-portfolio

# Install Tailwind CSS (Vite plugin)
npm install tailwindcss @tailwindcss/vite
